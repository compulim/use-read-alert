name: Continuous deployment

on:
  push:
    branches: [main]
    paths-ignore:
      - '.devcontainer/**'
      - '.github/**'

jobs:
  call-workflow:
    uses: compulim/workflows/react/continuous-deployment.yml
    with:
      package-name: use-read-alert
